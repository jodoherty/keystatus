# Key Status for i3blocks

As a sticky keys user, I needed some way to report the current status of
various modifier keys. This program provides that.

Example i3blocks config:

```
[keystatus]
command=keystatus
label=KEY
interval=persist
```
